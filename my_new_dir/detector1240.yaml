---
api_version: v1
uuid: 6561
name: WIN-CMD-BEAST-MODE
state: active_being_tuned
context:
  description: This is a description
  normal_behavior_notes: This is NORMAL behavior
  malicious_behavior_notes: This is MALICIOUS behavior
  reference_urls: these are the urls
attack_techniques_involved:
  explicitly_used:
  - T1059.003
  - T1547.001
  conditionally_used: []
threat_intelligence: none
suppression_control:
  allow_suppression: false
  allow_suppression_with_option: true
reported_process_depth: self
scoring_control:
  override_expected_impact: 'false'
detections:
  red_canary_behavioral:
    matcher: "|- windows? && process_path_is_likely_windows? && process_is_likely?('cmd')
      && process_command_line_likely_matches_all?('xcopy', 'startup') && process_command_line_includes_at_least_x?(count:2,
      possible_matches: ['&', '&&'])"
